version: '3.4'
services:
  database:
    image: "mysql:5.7"
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: w2e3r4T5
      MYSQL_DATABASE: TutAPI
      MYSQL_USER: animtel
      MYSQL_PASSWORD: w2e3r4T5

  server:
    depends_on:
      - database
    image: "animtel/nordapi:v2.0"
    ports:
      - "80:80"

volumes:
  db_data: